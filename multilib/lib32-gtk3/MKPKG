arch=('x86_64')
pkgname=('lib32-gtk3')
builddeps=('lib32-at-spi2-atk' 'lib32-colord' 'lib32-json-glib' 'lib32-rest'
           'lib32-libxkbcommon-x11' 'lib32-libxkbcommon')

build () {
    shift
    ionice -c 3 chrt -b 0 sudo multilib-build -- $*
}

# vim: ts=2 sw=2 et:
