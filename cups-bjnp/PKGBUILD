# Maintainer: Alucryd <alucryd at gmail dot com>
# Contributor: royrocks <royrocks13 at gmail dot com>

pkgname=cups-bjnp
pkgver=1.2
pkgrel=1
pkgdesc="CUPS backend for the canon proprietary network printing protocol, BJNP"
arch=('i686' 'x86_64')
url="http://sourceforge.net/projects/cups-bjnp/"
license=('GPL')
depends=('libcups')
source=(http://downloads.sourceforge.net/sourceforge/$pkgname/$pkgname-$pkgver.tar.gz)
sha256sums=('db629808e6e243f24114eb29f0164b2495c8cb1d8c4a11131a87c2fc7c52c6dd')

build() {
  cd "${srcdir}"/${pkgname}-${pkgver}
  ./configure --prefix=/usr
  make
}

package() {
  cd "${srcdir}"/${pkgname}-${pkgver}
  make DESTDIR="${pkgdir}" install
}

# vim: ts=2 sw=2 et:
